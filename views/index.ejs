<!DOCTYPE html>
<html>
  <head>
    <title>Timestamp Microservice</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Timestamp Microservice</h1>
    <p>If you pass a string as a parameter, and it will check to see whether that string contains either a unix timestamp or a natural language date (example: January 1, 2016)</p>
    <p>If it does, it returns both the Unix timestamp and the natural language form of that date.</p>
    <p>If it does not contain a date or Unix timestamp, it returns null for those properties.</p>
    <p>Example usage:</p>
    <p>https://timestamp-reformatt.herokuapp.com/December%2015,%202015</p>
    <p> https://timestamp-reformatt.herokuapp.com/1450137600</p>
    <p>Example output:</p>
    <p>{ "unix": 1450137600, "natural": "December 15, 2015" }</p>
  </body>
</html>
